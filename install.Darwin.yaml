- defaults:
    link:
      relink: true
      create: true

- link:
    ~/.config/karabiner: karabiner
    ~/.config/kitty/mac.conf: kitty/kitty-mac.conf
    ~/.finicky.js: finicky.js
    ~/.gnupg/gpg-agent.conf: gpg-agent-mac.conf
    ~/Library/Preferences/com.knollsoft.Rectangle.plist: rectangle.plist
