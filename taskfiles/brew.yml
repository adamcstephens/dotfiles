version: "3"

tasks:
  bundle:
    cmds:
      - brew bundle

  bundle-dump:
    cmds:
      - brew bundle dump --all --force
      - git diff Brewfile

  install:
    cmds:
      - ~/.dotfiles/bin/install-brew.sh
