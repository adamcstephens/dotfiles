version: "3"

tasks:
  build:
    cmds:
      - earthly +build
    status:
      - docker images | rg dotfiles-test

  shell:
    cmds:
      - task: build
      - docker run --rm -it dotfiles-test fish
