- defaults:
    link:
      relink: true
      create: true

- create:
    - ~/.tmux/plugins
    - ~/.vim
    - ~/notes
    - ~/projects
    - ~/tmp

- link:
    ~/.config/bat/config: bat
    ~/.config/kitty/kitty.conf: kitty.conf
    ~/.finicky.js:
      if: '[ `uname` = Darwin ]'
      path: finicky.js
    ~/.gitconfig: gitconfig
    ~/.giteditor.sh: giteditor.sh
    ~/.gitignore: gitignore
    ~/.gnupg/gpg-agent.conf:
      if: 'which gpg-agent'
      path: gnupg/gpg-agent.conf
    ~/.gnupg/gpg.conf:
      if: 'which gpg-agent'
      path: gnupg/gpg.conf
    ~/.inputrc:
      if: '[ ! -e /etc/inputrc ]'
      path: inputrc
    ~/.irbrc:
      if: 'which irb'
      path: irbrc
    ~/.screenrc: screenrc
    ~/.tmux.conf:
      if: 'which tmux'
      path: tmux.conf
    ~/.toprc: toprc
    ~/.tzvt_config:
      if: 'which tmux'
      path: tzvt_config
    ~/.vimrc: vimrc
    ~/.xprofile:
      if: '[ `uname` = Linux ] && which X'
      path: xprofile
