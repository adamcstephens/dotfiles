(rule
 (with-stdout-to
  link_flags.linux
  (bash
   "if [ \"${BUILD_STATIC:-0}\" = '1' ]; then echo '(-cclib -static)'; else echo '()'; fi")))

(executables
 (names escape_file wayland_locker)
 (public_names escape-file wayland-locker)
 (libraries unix fileutils yojson)
 (link_flags
  (:include link_flags.%{system})))
