#!/usr/bin/env bash

SSH_AUTH_SOCK="$HOME/.ssh/ssh-auth.sock"
if [ -n "$XDG_RUNTIME_DIR" ]; then
  SSH_AUTH_SOCK="$XDG_RUNTIME_DIR/ssh-auth.sock"
fi
export SSH_AUTH_SOCK

if [ -d .jj ]; then
  exec jjui "$@"
fi

if [ -d .git ]; then
  exec lazygit "$@"
fi
