#!/usr/bin/env bash

unset DISPLAY

if command -v emacsclient >/dev/null 2>&1 && pgrep emacs; then
  export IS_EDITOR=true
  exec em $@
elif command -v nvim 2>&1; then
  exec nvim $@
elif command -v vim 2>&1; then
  exec vim $@
else
  echo "No supported editor found!"
  exit 1
fi
