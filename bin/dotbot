#!/usr/bin/env sh

: "${CONFIG:="dotbot.conf.yaml"}"

BASEDIR="${HOME}/.dotfiles"

cd "${BASEDIR}" || exit 2

# delete some stock files
[ -h "$HOME/.bashrc" ] || rm -fv "$HOME/.bashrc"
[ -h "$HOME/.bash_profile" ] || rm -fv "$HOME/.bash_profile"

"${BASEDIR}/vendor/dotbot/bin/dotbot" --quiet -d "${BASEDIR}" -c "${CONFIG}" "${@}"
