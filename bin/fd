#!/usr/bin/env sh

# avoid a loop
SCRIPT_PATH=$(dirname "$(realpath "$0")")
PATH="$(echo "$PATH" | sed -e "s,$SCRIPT_PATH:,,")"
export PATH

fdopts="--ignore-case --hidden --follow"

if command fdfind >/dev/null 2>&1; then
  fdfind $fdopts "$@"
else
  exec fd $fdopts "$@"
fi
